# <Paper title>

- **arXiv id:** 
- **Date:** 
- **URL:** 
- **Taxonomy tags:** (forms / functions / dynamics, per `research/taxonomy.md`)

## Problem

What gap in agent memory does this paper address?

## Mechanism

How does it work, in a few sentences? Diagrams welcome.

## What's reusable for corticore

Concretely, what could this change or add in `src/corticore/`? Name the
module(s).

## Setup cost

Does adopting this idea require new infrastructure (a graph DB, a specific
model, GPU, etc.)? corticore's default path must stay zero-setup — note
whether this belongs behind an optional extra instead.

## Decision

- [ ] Adopt now → link the ADR that will record it
- [ ] Defer → why, and what would change that
- [ ] Reject → why

Update this paper's `status` in `research/papers.yaml` to match.
